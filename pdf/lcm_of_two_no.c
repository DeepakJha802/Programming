#include <stdio.h>
int main (void)
{
    int a , b , lcm = 0 ;
    printf("\nEnter Two Numbers Which You Have Two Find LCM : ");
    scanf("%d %d",&a,&b);
    if (a>=b)
    {
        for (int i=0 ; i <= a/2 ; i++ )
        lcm =
    }
}
